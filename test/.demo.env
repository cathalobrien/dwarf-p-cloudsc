#simple toy run, just to provide a template of how runs can be defined
#this scritp will do a total of 2 runs (gnu 8.5.0 sp & dp) across an entire node

CPUS_PER_NUMA_REGION=16
CPUS_PER_SOCKET=64
CPUS_PER_NODE=128
#NUM_THREADS=($CPUS_PER_NUMA_REGION $CPUS_PER_SOCKET $CPUS_PER_NODE)
NUM_THREADS=($CPUS_PER_NODE)

NCOLS=(131072)
NPROMA_SIZES=(32)
iterations=1

#openmp x mpi geometry 
THREAD_TASK_RATIO=(4) #how many threads each task will be allocated, 4 is the best on AMD Rome due to shared L3 cache by 4 cores

#The different builds to be benchmarked
ARCH_FILES=("ecmwf/hpc2020/gnu/8.5.0")
BUILD_FLAGS=("" "--single-precision")
